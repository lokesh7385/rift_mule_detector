services:
  - type: web
    name: rift-mule-detector
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --chdir backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
